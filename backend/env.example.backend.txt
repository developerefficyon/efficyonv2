# Server-side Supabase config for the Node/Express backend
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Other backend secrets / config
JWT_SECRET=your-jwt-secret
PORT=4000

# Fortnox OAuth Configuration
# Redirect URI for Fortnox OAuth callback
# For local development: http://localhost:4000/api/integrations/fortnox/callback
# For production: https://your-backend-service.onrender.com/api/integrations/fortnox/callback
FORTNOX_REDIRECT_URI=http://localhost:4000/api/integrations/fortnox/callback

# Frontend app URL (used for OAuth redirects after callback)
FRONTEND_APP_URL=http://localhost:3000

# Fortnox OAuth Scope (optional)
# Default: "companyinformation settings profile archive inbox invoice supplierinvoice bookkeeping salary article customer supplier"
# Valid scope names per Fortnox API: archive, companyinformation, inbox, profile, settings, etc.
# Scopes that work with "Any" license: archive, companyinformation, inbox, profile, settings
# Scopes that require licenses: customer (Kundfaktura/Order), invoice (Order/Kundfaktura), order (Order), etc.
# Multiple scopes: "companyinformation settings profile archive inbox invoice supplierinvoice bookkeeping salary article customer supplier"
# See Fortnox docs for full list: https://developer.fortnox.se/documentation/oauth/scopes/
FORTNOX_OAUTH_SCOPE=companyinformation settings profile archive inbox invoice supplierinvoice bookkeeping salary article customer supplier
# Stripe Configuration
# Get these from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your_secret_key_here
STRIPE_WEBHOOK_SECRET=whsec_test_your_webhook_secret_here

# OpenAI Configuration for AI-powered analysis
# Get API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk_your_openai_key_here
# Optional: specify model (default: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# Resend Email Configuration
# Get API key from https://resend.com/api-keys
RESEND_API_KEY=re_your_resend_api_key_here
# From email address (must be verified in Resend)
RESEND_FROM_EMAIL=onboarding@resend.dev
# Optional: From name
RESEND_FROM_NAME=Efficyon
